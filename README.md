# accessX
A Python library for assessing X-minute accessibility that computes network-based access from grids to categorized POIs with pluggable cost models and optional equity accounting —OSM-first, but data-agnostic
